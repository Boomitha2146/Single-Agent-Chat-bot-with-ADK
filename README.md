# Single-Agent-Chat-bot-with-ADK
Single Agent Chatbot built using Agent Development Kit (ADK) that independently understands user queries, maintains context, and generates intelligent responses using an LLM.
